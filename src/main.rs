use std::io;
use deckofcards::*;

fn main() {
    println!("Welcome to HiLo!");

    //prep the game
    let mut deck = Deck::new();
    deck.shuffle();


    //first round

    //second round
    let mut group_score = 0;

    //output score
}
